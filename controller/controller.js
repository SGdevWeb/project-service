const project = require("./projectController");
const comment = require("./commentController");
const collaborators = require("./collaboratorsController");

module.exports = {
    project,
    comment,
    collaborators,
}